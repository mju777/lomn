<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="37"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="37"
        tools:ignore="ScopedStorage" />

    <application

        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity
            android:exported="true"
            android:name="me.zackyu.yubook.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="me.zackyu.yubook.NewRecordActivity" />
        <activity android:name="me.zackyu.yubook.RecordsActivity" />
        <activity android:name="me.zackyu.yubook.NewPayActivity" />
        <activity android:name="me.zackyu.yubook.NewIncomeActivity" />
        <activity android:name="me.zackyu.yubook.AboutMeActivity" />
        <activity android:name=".QuicklyRecordActivity" />
    </application>

</manifest>